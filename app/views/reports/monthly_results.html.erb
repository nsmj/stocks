<%= turbo_frame_tag 'monthly_results' do %>
    <table style="width: 100%">
    <thead>
        <tr>
            <th>Data</th>
            <th>Operação</th>
            <th>Ativo</th>
            <th class="text-end">Quantidade</th>
            <th class="text-end">Preço</th>
            <th class="text-end">Taxas</th>
            <th class="text-end">Valor Operação</th>
            <th class="text-end">Lucro Líquido</th>
        </tr>
        </thead>
        <tbody>
            <% @results.each do |result| %>
            <tr>
                <td><%= result['trade_date'] %></td>
                <td><%= result['purchase'] == 1 ? "Compra" : "Venda" %></td>
                <td><%= result['ticker'] %></td>
                <td class="text-end"><%= result['quantity'] %></td>
                <td class="text-end"><%= to_currency(result['asset_price']) %></td>
                <td class="text-end"><%= to_currency(result['fees']) %></td>
                <td class="text-end"><%= to_currency(result['total_amount']) %></td>
                <td class="text-end"><%= to_currency(result['net_profit']) %></td>
            </tr>
            <% end %>
        <tr>
            <td colSpan="7" class="text-end" style="padding: 5px">Total</td>
            <td>
                <%= to_currency(@total) %>
            </td>
        </tr>
        </tbody>
    </table>
<% end %>