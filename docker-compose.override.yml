services:
  web:
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: nodemon --exec "rails s -b 0.0.0.0"
    environment:
      - RUBY_DEBUG_OPEN=true
    stdin_open: true
    tty: true